package dev.origami.server;

/**
 * Server-side home for Origami internals that don't belong in a patch
 * (helpers, config loading, etc.). Prefer adding plain source here over
 * patching Paper/Minecraft files whenever you can.
 */
public final class OrigamiServer {
    private OrigamiServer() {
    }
}
